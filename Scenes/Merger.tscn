[gd_scene format=3 uid="uid://bv0l8komr0yj5"]

[sub_resource type="GDScript" id="GDScript_26akd"]
script/source = "extends Node2D

@onready var area: Area2D = $Area2D

var is_placed := false
var current_direction := 0 
var speed := 64.0

var output_route = Vector2(0, -1) 

# --- NEW: Identify what item this building costs ---
@export var building_item_id: String = \"merger\"

# --- PERFECT ZIPPER TRACKING ---
var active_items = {} 
var queue = []
var center_occupant: Area2D = null

func _ready() -> void:
	if not is_placed:
		BuildManager.current_preview = self
		modulate.a = 0.5
	else:
		area.area_entered.connect(_on_area_entered)

# --- NEW: 1x1 Building footprint ---
func get_occupied_cells(center_cell: Vector2i) -> Array[Vector2i]:
	return [center_cell]

func _process(_delta: float) -> void:
	if is_placed: return
		
	var mouse_pos = get_global_mouse_position()
	var current_grid_cell = GridManager.world_to_grid(mouse_pos)
	global_position = GridManager.grid_to_world(current_grid_cell)

	var cells_to_check = get_occupied_cells(current_grid_cell)
	
	# --- UPDATED: GridManager checks if the physical space is clear AND if inventory has stock ---
	if GridManager.is_placement_blocked(cells_to_check, building_item_id):
		modulate = Color(1.0, 0.4, 0.4, 0.8) # Red if blocked
	else:
		modulate = Color(1.0, 1.0, 1.0, 0.5) # White if clear

func _unhandled_input(event: InputEvent) -> void:
	if not is_placed:
		if event is InputEventKey and event.keycode == KEY_R and event.pressed:
			rotation_degrees += 90
			current_direction = (current_direction + 1) % 4
			
		if event is InputEventMouseButton and event.button_index == MOUSE_BUTTON_LEFT and event.pressed:
			var current_grid_cell = GridManager.world_to_grid(get_global_mouse_position())
			
			var cells_to_claim = get_occupied_cells(current_grid_cell)
			
			# --- UPDATED: GridManager handles checking the inventory AND deducting the item! ---
			if GridManager.place_item(cells_to_claim, self):
				is_placed = true
				modulate = Color(1.0, 1.0, 1.0, 1.0) # Reset color fully back to normal
				area.area_entered.connect(_on_area_entered)
				
				var next_merger = load(scene_file_path).instantiate()
				next_merger.current_direction = current_direction
				next_merger.rotation_degrees = rotation_degrees
				get_parent().add_child(next_merger)

func _on_area_entered(hit_area: Area2D) -> void:
	if hit_area.is_in_group(\"items\") and not active_items.has(hit_area):
		hit_area.set_physics_process(false) 
		active_items[hit_area] = {\"state\": \"waiting\"}
		queue.append(hit_area)

func _physics_process(delta: float) -> void:
	if not is_placed: return
	
	var center = global_position
	var world_output_dir = output_route.rotated(global_rotation).round()
	
	# --- 1. CLEANUP ---
	var keys = active_items.keys()
	for item in keys:
		if not is_instance_valid(item):
			active_items.erase(item)
			queue.erase(item)
			if center_occupant == item: center_occupant = null

	# --- 2. ZIPPER CONTROLLER ---
	# Green light for the next item the exact millisecond the previous one is 24 pixels away.
	if center_occupant == null or center_occupant.global_position.distance_to(center) >= 24.0:
		while queue.size() > 0:
			var next_up = queue.pop_front()
			if is_instance_valid(next_up):
				center_occupant = next_up
				active_items[center_occupant][\"state\"] = \"moving_to_center\"
				break

	# --- 3. MOVEMENT LOGIC ---
	for item in active_items.keys():
		var data = active_items[item]
		
		if data[\"state\"] == \"moving_to_center\":
			# Moves directly to the center. Bumper checks aren't needed here because the queue spaces them out perfectly.
			item.global_position = item.global_position.move_toward(center, speed * delta)
			
			if item.global_position.distance_to(center) < 1.0:
				item.global_position = center
				data[\"state\"] = \"moving_to_edge\"
				
		elif data[\"state\"] == \"moving_to_edge\":
			var edge_target = center + (world_output_dir * 32.0)
			
			# ONLY check the slim bumper to see if the immediate space ahead is clear
			if is_path_clear(item, world_output_dir):
				item.global_position = item.global_position.move_toward(edge_target, speed * delta)
				
				if item.global_position.distance_to(edge_target) < 1.0:
					item.global_position = edge_target
					item.set_physics_process(true)
					active_items.erase(item)

# --- Utilities ---
func is_path_clear(item: Area2D, world_dir: Vector2) -> bool:
	var space_state = get_world_2d().direct_space_state
	var query = PhysicsShapeQueryParameters2D.new()
	var shape = RectangleShape2D.new()
	
	# Slimmed-down bumper to prevent false positives
	if world_dir.x != 0:
		shape.size = Vector2(4, 20)
	else:
		shape.size = Vector2(20, 4)
		
	query.shape = shape
	query.transform = Transform2D(0, item.global_position + (world_dir * 24.0))
	query.collide_with_areas = true
	query.exclude = [item.get_rid()]
	
	var blockers = space_state.intersect_shape(query)
	for b in blockers:
		if b.collider.is_in_group(\"items\"):
			if active_items.has(b.collider):
				continue # Items inside the merger ignore each other
			return false
	return true
"

[sub_resource type="CompressedTexture2D" id="CompressedTexture2D_tfh0r"]
load_path = "res://.godot/imported/Merger.png-315c0ab77471da7f6db00cdd111699a9.ctex"

[sub_resource type="AtlasTexture" id="AtlasTexture_hqv6x"]
atlas = SubResource("CompressedTexture2D_tfh0r")
region = Rect2(0, 0, 64, 64)

[sub_resource type="AtlasTexture" id="AtlasTexture_wamn6"]
atlas = SubResource("CompressedTexture2D_tfh0r")
region = Rect2(64, 0, 64, 64)

[sub_resource type="AtlasTexture" id="AtlasTexture_qwmcy"]
atlas = SubResource("CompressedTexture2D_tfh0r")
region = Rect2(128, 0, 64, 64)

[sub_resource type="AtlasTexture" id="AtlasTexture_ybukm"]
atlas = SubResource("CompressedTexture2D_tfh0r")
region = Rect2(192, 0, 64, 64)

[sub_resource type="AtlasTexture" id="AtlasTexture_h0jaf"]
atlas = SubResource("CompressedTexture2D_tfh0r")
region = Rect2(0, 64, 64, 64)

[sub_resource type="AtlasTexture" id="AtlasTexture_0h5wg"]
atlas = SubResource("CompressedTexture2D_tfh0r")
region = Rect2(64, 64, 64, 64)

[sub_resource type="AtlasTexture" id="AtlasTexture_mpsfi"]
atlas = SubResource("CompressedTexture2D_tfh0r")
region = Rect2(128, 64, 64, 64)

[sub_resource type="AtlasTexture" id="AtlasTexture_60lvv"]
atlas = SubResource("CompressedTexture2D_tfh0r")
region = Rect2(192, 64, 64, 64)

[sub_resource type="AtlasTexture" id="AtlasTexture_3n1rh"]
atlas = SubResource("CompressedTexture2D_tfh0r")
region = Rect2(0, 128, 64, 64)

[sub_resource type="AtlasTexture" id="AtlasTexture_e6x5w"]
atlas = SubResource("CompressedTexture2D_tfh0r")
region = Rect2(64, 128, 64, 64)

[sub_resource type="AtlasTexture" id="AtlasTexture_rf5ny"]
atlas = SubResource("CompressedTexture2D_tfh0r")
region = Rect2(128, 128, 64, 64)

[sub_resource type="AtlasTexture" id="AtlasTexture_qxed0"]
atlas = SubResource("CompressedTexture2D_tfh0r")
region = Rect2(192, 128, 64, 64)

[sub_resource type="AtlasTexture" id="AtlasTexture_4ksbq"]
atlas = SubResource("CompressedTexture2D_tfh0r")
region = Rect2(0, 192, 64, 64)

[sub_resource type="AtlasTexture" id="AtlasTexture_fjkxw"]
atlas = SubResource("CompressedTexture2D_tfh0r")
region = Rect2(64, 192, 64, 64)

[sub_resource type="AtlasTexture" id="AtlasTexture_t02jy"]
atlas = SubResource("CompressedTexture2D_tfh0r")
region = Rect2(128, 192, 64, 64)

[sub_resource type="AtlasTexture" id="AtlasTexture_3xrp8"]
atlas = SubResource("CompressedTexture2D_tfh0r")
region = Rect2(192, 192, 64, 64)

[sub_resource type="SpriteFrames" id="SpriteFrames_tlqat"]
animations = [{
"frames": [{
"duration": 1.0,
"texture": SubResource("AtlasTexture_hqv6x")
}, {
"duration": 1.0,
"texture": SubResource("AtlasTexture_wamn6")
}, {
"duration": 1.0,
"texture": SubResource("AtlasTexture_qwmcy")
}, {
"duration": 1.0,
"texture": SubResource("AtlasTexture_ybukm")
}, {
"duration": 1.0,
"texture": SubResource("AtlasTexture_h0jaf")
}, {
"duration": 1.0,
"texture": SubResource("AtlasTexture_0h5wg")
}, {
"duration": 1.0,
"texture": SubResource("AtlasTexture_mpsfi")
}, {
"duration": 1.0,
"texture": SubResource("AtlasTexture_60lvv")
}, {
"duration": 1.0,
"texture": SubResource("AtlasTexture_3n1rh")
}, {
"duration": 1.0,
"texture": SubResource("AtlasTexture_e6x5w")
}, {
"duration": 1.0,
"texture": SubResource("AtlasTexture_rf5ny")
}, {
"duration": 1.0,
"texture": SubResource("AtlasTexture_qxed0")
}, {
"duration": 1.0,
"texture": SubResource("AtlasTexture_4ksbq")
}, {
"duration": 1.0,
"texture": SubResource("AtlasTexture_fjkxw")
}, {
"duration": 1.0,
"texture": SubResource("AtlasTexture_t02jy")
}, {
"duration": 1.0,
"texture": SubResource("AtlasTexture_3xrp8")
}],
"loop": true,
"name": &"default",
"speed": 60.0
}]

[sub_resource type="RectangleShape2D" id="RectangleShape2D_sgxja"]
size = Vector2(64, 64)

[node name="Merger" type="Node2D" unique_id=104790272]
script = SubResource("GDScript_26akd")

[node name="AnimatedSprite2D" type="AnimatedSprite2D" parent="." unique_id=601414390]
rotation = 3.1415927
sprite_frames = SubResource("SpriteFrames_tlqat")
autoplay = "default"

[node name="Area2D" type="Area2D" parent="." unique_id=540902078]

[node name="CollisionShape2D" type="CollisionShape2D" parent="Area2D" unique_id=1029190232]
shape = SubResource("RectangleShape2D_sgxja")
